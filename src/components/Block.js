import styled from "styled-components";

export default styled.div`
    background-color: #ffffff;
    padding: 64px;
    max-width: 560px;
    margin: 0 auto;
    box-shadow: 40px 40px 50px rgba(0, 0, 0, 0.08);
    display: grid;
    grid-template-columns: 1fr;
    grid-template-rows: auto;
    grid-row-gap: 32px;
`;
